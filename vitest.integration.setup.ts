// vitest.integration.setup.ts
import { beforeAll, afterAll } from 'vitest';

// Setup for Astro integration tests
beforeAll(async () => {
  // Any global setup for integration tests
});

afterAll(async () => {
  // Any global teardown
});